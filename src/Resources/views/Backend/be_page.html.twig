{% set _main  = block('main') %}
{% set _headline = block('headline') %}
{% set _error = block('error') %}

{{ contao_base_template({
    main: _main,
    error: _error,
    headline: _headline
}) | raw }}
